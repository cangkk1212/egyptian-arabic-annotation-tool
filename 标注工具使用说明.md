# 埃及方言音频标注工具使用说明

> 本说明面向标注人员，介绍工具的使用方法和每一个功能按键。请在工作前通读一遍。

## 目录

1. [工具简介](#1-工具简介)
2. [登录](#2-登录)
3. [界面总览](#3-界面总览)
4. [标准标注流程](#4-标准标注流程)
5. [功能按键详解](#5-功能按键详解)
6. [自动保存机制](#6-自动保存机制)
7. [快捷键](#7-快捷键)
8. [文件状态与任务分配规则](#8-文件状态与任务分配规则)
9. [常见提示与解决办法](#9-常见提示与解决办法)
10. [注意事项](#10-注意事项)

---

## 1. 工具简介

本工具用于**阿拉伯语（埃及方言）音频**的转写标注。系统已用 VAD 自动将音频切分为若干语音段，并用 ASR 模型生成了预标注文本。您的工作是：

1. **听**每一段音频；
2. **核对/修改**转写文本；
3. 对不合格的段落或音频做相应标记；
4. 全部完成后提交，系统自动分配下一个文件。

**访问方式**：使用 Chrome / Edge / Firefox 等现代浏览器，打开管理员提供的网址。
当前公网地址：https://arabic-annotation.top/

---

## 2. 登录

打开网址后进入登录页：
![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026081312280938/48813710/199a72c0f67d406e8c1b9625a3509d9d.png)

| 元素 | 说明 |
|------|------|
| **姓名输入框**（Your Name） | 输入您的姓名，点击 **Join** 按钮（或按回车）登录 |
| **Join 按钮** | 提交姓名并进入标注界面 |
| **排行榜**（📊 Annotation Dashboard） | 登录页下方显示所有标注员的贡献统计 |

**姓名规则（重要）**：

- 请**始终使用同一个名字**登录，您的所有标注贡献都会记录在这个名字下；
- 姓名最多 50 个字符，只能包含字母、数字、空格、点、连字符、下划线；
- 同一用户名仅支持单设备登陆。如果提示 `'xxx' is already in use`，说明该名字当前已被其他设备使用。请在已登录设备执行Logout，即可在其它设备正常登录。

---

## 3. 界面总览

登录后进入标注主界面，从上到下、从左到右依次为：
![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026081312280938/48818302/e5daf466290d43eeadfcb5eab65aebc2.png)

---

## 4. 标准标注流程

对每个分配到的音频文件，按以下步骤操作：

1. 顶部音频播放器可完整播放，了解内容；
2. **逐段标注**：从上到下处理表格中的每一段：
   - 点击该段的 **▶ 播放** 按钮听音频；
   - 在 **Text** 文本框中核对并填写正确的转写（灰色文字是 ASR 预标注参考）；
   - 必要时用 **Bad Quality** 标记质量差的段；
3. **点击 ✓ Mark Done** 提交，系统自动保存并分配下一个文件；
4. 如果整个音频无法使用（噪声过大 / 不是埃及方言 / 音质太差），点击对应的 **Skip** 按钮，该文件将以"跳过"状态提交，并自动加载下一个文件。

注意：当点击Mark Down提交时需保证所有text都已填写完成，否则会报错。或者可将某一音频段标记为Bad Quality，则可空白text完成Mark Done提交。

---

## 5. 功能按键详解

### 5.1 顶部导航栏

| 按键/元素 | 功能详解 |
|-----------|----------|
| **📌 任务信息**（Assigned / Resumed） | 显示当前分配的文件名和剩余待处理文件数。`Assigned` 表示新分配的文件；`Resumed` 表示继续处理之前分配的文件（如刷新页面后）。 |
| **User（用户名）** | 显示您当前登录的名字 |
| **Logout（退出登录）** | 退出登录，**立即释放**当前分配的文件（已保存的内容不受影响）。请完成当前文件后再退出。 |
| **Confirmed（已确认）** | 全部库中已完成标注（Mark Done）的文件总数 |
| **Skipped（已跳过）** | 全部库中以跳过状态提交的文件总数 |
| **Pending（待标注）** | 尚未处理的文件数 |
| **Total（总数）** | 音频文件总数 |

> 顶部统计数字来自服务器全局数据，提交文件后会实时刷新。

### 5.2 工具栏（页面顶部第二行）

| 按键 | 功能详解 |
|------|----------|
| **文件名 / 时长·段数** | 显示当前文件的名称、总时长（秒）和语音段数量 |
| **音频播放器** | 浏览器原生播放器，**连续播放**整个音频文件（不会在段尾自动停止）。拖动进度条可跳转，波形图上的红色播放线会同步跟随 |
| **Skip 跳过按钮组** | 三个按钮用于**提交整个文件为"跳过"**，详见下方说明 |

#### Skip 跳过按钮（⚠ 点击即提交）

这三个按钮不是普通标记，**点击后会立即将当前文件以"跳过"状态提交，并自动加载下一个文件**：

| 按键 | 使用场景 |
|------|----------|
| **Noisy（噪声过大）** | 音频背景噪声过大，无法正常听清 |
| **Not Egyptian（非埃及方言）** | 音频内容不是埃及方言（如其他方言、标准阿拉伯语或其他语言） |
| **Poor Quality（音质太差）** | 录音音质太差（如杂音、失真、音量过小等） |

- 三个原因**互斥**，同一文件只能选一个；
- **误点怎么办**：提交后会跳到下一个文件，此时点击 **↩ Back** 可返回该文件，文件会恢复可编辑状态，您可重新标注后点 **✓ Mark Done** 覆盖为"已完成"；
- 点击已激活的跳过按钮（在通过 Back 返回的文件上）只会取消高亮，不会改变文件状态。

#### 其他工具栏按键

![](https://wiki.huawei.com/vision-file-storage/api/file/download/upload-v2/WIKI2026081312280938/48816462/b173588deeba4aed9416a253c7cf800a.png)

| 按键 | 功能详解 |
|------|----------|
| **↩ Back（返回）** | 返回**上一个**您处理过的文件（保留最近 10 个历史）。用于误操作后回改。返回的文件保持其原有状态（已完成/跳过）且**恢复可编辑**，您修改后需**再次点击 Mark Done 或 Skip 才会重新提交生效** |
| **Next ▶（前进）** | 在 Back 之后使用，可继续前进回到最新处理的文件 |
| **💾 Save（保存）** | 手动保存当前文件的所有修改（转写文本、时间段调整）。正常情况下系统会自动保存，此按钮用于放心确认 |
| **✓ Mark Done（完成提交）** | **核心提交按钮**。校验并保存后，将当前文件标记为"已完成标注"，自动加载下一个文件（若是在 **Back 返回的文件**上重新提交，则停留在当前文件，可用 **Next ▶** 继续前进）。点击前会检查：所有未标记 Bad Quality 的段落都必须有转写文本，否则弹窗提示缺失的段号，不会提交 |

### 5.3 段落表格（每个语音段一行）

| 列 | 按键/元素 | 功能详解 |
|----|-----------|----------|
| ▶ | **播放/暂停按钮** | 点击后从该段**起点**开始播放，播放到**段尾自动暂停**；再次点击暂停。播放中按钮显示 ⏸，加载中显示 ⏳ |
| # | 段编号 | 语音段序号，与波形图上的编号对应 |
| Start | **开始时间输入框** | 可修改该段的开始时间。格式：`分:秒.毫秒`（如 `1:23.456`）或纯秒数（如 `83.456`）。受约束：**不能早于上一段的结束时间**，且必须小于本段结束时间。越界时自动截断并弹提示。鼠标悬停可查看允许的时间范围 |
| End | **结束时间输入框** | 同上，约束为：不能晚于下一段的开始时间，且必须大于本段开始时间 |
| Duration | 段时长 | 自动计算显示（秒），修改 Start/End 后即时更新，不可直接编辑 |
| Text | **转写文本框** | 填写/修改该段的阿拉伯语转写文本（**从右向左书写**）。详见下方说明 |
| Quality | **Bad Quality（质量差）按钮** | 将该段标记为"质量差、不参与训练"（`exclude_from_training`）。激活后按钮变红。**立即保存**。标记了 Bad Quality 的段落**无需填写文本**即可提交 |

#### Text 转写文本框详解

- **ASR 预标注参考**：文本框中的文字是预识别的结果。⚠ **它只是参考，不算已标注**——您必须听音频核对后，在文本框中确认正确文本；
- **点击文本框聚焦**：音频自动跳到该段起点，该行高亮，波形图滚动到对应位置，方便边听边写；
- **自动保存**：输入后 3 秒自动保存；点击其他位置（失焦）时也立即保存；
- **Tab / Shift+Tab**：在文本框中按 Tab 跳到下一段的文本框，Shift+Tab 回到上一段。

#### 时间输入规则

- 时间不能与其他段**重叠**：调整某段的 Start/End 时，只能在其相邻两段之间伸缩；
- 修改时间后，该段 Duration 自动重算，波形图同步更新；
- 已提交（锁定）的文件中时间输入框为灰色不可编辑（见第 8 节）。

### 5.4 波形图

位于段落表格下方，是整个音频的可视化视图：

| 元素 | 功能详解 |
|------|----------|
| **对称波形** | 波形上下对称显示，便于观察音量变化和语音分布 |
| **段落背景块** | 每个浅紫色圆角块 = 一个语音段；块上方显示该段的文本预览（太长自动省略），下方显示 **#段号** 和**时长** |
| **当前段高亮** | 正在播放/编辑的段显示为亮紫色高亮 |
| **点击跳转** | 点击波形任意位置，音频跳到该时间点并开始播放；点击某个**段块内部**，则播放该段（段尾自动停） |
| **红色播放线** | 表示当前播放位置，随播放移动，波形自动滚动跟随 |
| **横向滚动** | 长音频的波形很宽，可拖动底部滚动条查看；播放时自动滚动到当前播放位置 |

### 5.5 右侧边栏：排行榜（Annotation Dashboard）

| 元素 | 功能详解 |
|------|----------|
| **◀ / ▶ 折叠按钮** | 点击收起/展开侧边栏 |
| **排行榜表格** | 按标注时长降序显示所有标注员：排名、用户名、已完成文件数、标注时长（小时）。**您自己的行**会高亮显示 |
| **自动刷新** | 每 30 秒自动刷新；窗口重新获得焦点时也立即刷新 |

---

## 6. 自动保存机制

| 时机 | 行为 |
|------|------|
| 修改转写文本后 3 秒 | 自动保存该段（失败会自动重试） |
| 文本框失焦（点击别处） | 立即保存该段 |
| 修改 Start/End 时间后 3 秒 | 自动保存该段 |
| 点击 Bad Quality 按钮 | 立即保存该段标记 |
| 点击 💾 Save | 立即保存所有未保存修改 |
| Mark Done / Skip 提交前 | 自动执行一次完整保存 |

> 保存失败时会自动重试；即使网络中断，已输入的内容也在本地保留，恢复后点击 💾 Save 即可。

---

## 7. 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + S`（Mac 为 `Cmd + S`） | 保存当前文件所有修改 |
| `Tab` / `Shift + Tab` | 在转写文本框之间切换到下一段 / 上一段 |
| `Enter`（登录页） | 提交姓名登录 |

---

## 8. 文件状态与任务分配规则

### 文件状态

| 状态 | 含义 | 如何进入该状态 |
|------|------|----------------|
| `pending`（待标注） | 已切分好段落、等待处理 | 默认状态 |
| `annotated`（已完成） | 标注完成，计入 Confirmed 统计 | 点击 **✓ Mark Done** |
| `skipped`（已跳过） | 整段音频不可用，计入 Skipped 统计 | 点击任一 **Skip 跳过按钮** |

**锁定规则**：文件提交后（Mark Done 或 Skip），转写文本框和时间输入框会变灰锁定。通过 **↩ Back** 返回的文件会**恢复可编辑**，但注意：修改后必须再次点击 **✓ Mark Done 或 Skip** 才会重新提交生效。重新提交后系统**不会跳到新文件**，而是停留在当前文件并保留 **Next ▶** 按钮，方便您继续前进回到最新处理的文件。

### 任务分配规则

- 登录后系统**随机自动分配**一个待处理文件（只会分配已预处理、有切分段的文件），**无需手动挑选**；
- 分配给您的文件会被锁定，其他标注员不会拿到，请放心慢慢标注；
- **会话超时**：30 分钟无操作后您的名字会被视为离线（他人可注册同名）。页面保持打开时会每 2 分钟自动发心跳保持在线；
- **分配超时**：如果您关闭浏览器离开超过 72 小时，分配的文件会自动释放回待分配池，您之前已保存的内容不受影响；
- **登出**：Logout 会立即释放当前文件（已提交状态保留，未提交的修改已自动保存）；
- **全部完成**：当没有可分配的文件时，页面显示 🎉 **All Done!**，并给出全局统计数字。若显示 `No preprocessed files available yet`，说明预处理尚未完成，请稍后刷新。

---

## 9. 常见提示与解决办法

| 提示信息 | 含义 | 处理方法 |
|----------|------|----------|
| `⚠ Segments 1, 3 have no text. Please annotate or mark as Bad Quality.` | 点击 Mark Done 时，段 1、3 的文本框为空 | 为这些段填写文本，或点击其 Bad Quality 按钮标记为质量差 |
| `Not before: 01:23.456` / `Not after: …` | 修改的时间超出了相邻段的边界 | 输入的时间已被自动截断到允许范围，检查相邻段是否合理 |
| `Audio load failed — retrying once...` | 音频加载失败，系统正在自动重试一次 | 等待重试；若仍失败，刷新页面 |
| `File is currently assigned to another annotator` | 您想 Back 返回的文件已被其他标注员领取 | 系统会自动放弃返回并给您分配一个新文件（提示：`↪ 原文件已被其他标注员领取，已为您分配新文件`） |
| `Network connection failed, please try again later` | 与服务器断网 | 检查网络，恢复后点击 💾 Save 重试 |
| `Save failed: …` | 保存出错 | 再次点击 💾 Save；持续失败请联系管理员 |
| `'xxx' is already in use. Please choose a different name.` | 该名字当前已被其它设备占用 | 换一个名字，或等待对方下线（30 分钟） |

---

## 10. 注意事项

1. **始终使用同一个名字登录**，您的贡献统计和标注记录都关联在这个名字下；
2. **ASR 预标注仅供参考**：灰色文字是机器识别结果，可能出错。务必听音频逐字核对，把正确文本输入文本框；
3. **不要依赖机器切分**：如发现段落切分位置不对（如把两个句子切成一段、或一句话被切断），请调整 Start/End 时间；
4. **跳过按钮是提交操作**：确认整个文件确实不可用时再点击，误点后用 ↩ Back 恢复；
5. **建议完成一个文件后再退出**：中途关闭页面不会丢数据（自动保存），但当前文件仍由您占用，重新登录后会显示 📌 Resumed 继续处理；
6. **遇到页面空白、反复报错等问题**：浏览器会自动把错误上报到服务器日志，请同时向管理员描述您的操作步骤和现象，便于排查。
